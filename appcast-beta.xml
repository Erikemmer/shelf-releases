<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>Shelf (beta)</title>
        <link>https://raw.githubusercontent.com/Erikemmer/shelf-releases/main/appcast-beta.xml</link>
        <description>Pre-release (rc) builds of Shelf.</description>
        <language>en</language>
        <!-- No pre-release has been published yet. make release fills this in for an rc version. -->
        <item>
            <title>1.1.0-rc2</title>
            <pubDate>Thu, 24 Sep 2026 08:19:38 +0200</pubDate>
            <sparkle:version>3</sparkle:version>
            <sparkle:shortVersionString>1.1.0-rc2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description>&lt;h2&gt;Shelf 1.1.0-rc2&lt;/h2&gt;
&lt;h2&gt;Sprint 15, Teil C — 1.1.0-rc2, the second release, proving a beta channel with two versions actually works · 23 September 2026&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;MARKETING_VERSION&lt;/code&gt; is &lt;code&gt;1.1.0-rc2&lt;/code&gt;, &lt;code&gt;CURRENT_PROJECT_VERSION&lt;/code&gt; is &lt;code&gt;3&lt;/code&gt; — higher than &lt;code&gt;1.1.0-rc1&lt;/code&gt;'s &lt;code&gt;2&lt;/code&gt;, as every build number must be. Still a pre-release: &lt;code&gt;Scripts/release.sh&lt;/code&gt; routes any &lt;code&gt;-rc&lt;/code&gt; version to the beta channel (&lt;code&gt;appcast-beta.xml&lt;/code&gt;) with &lt;code&gt;--prerelease&lt;/code&gt;, exactly as &lt;code&gt;1.1.0-rc1&lt;/code&gt; was.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;What this release actually is:&lt;/strong&gt; the code in Teil A (the false cancellation banner, fixed in two places) and Teil B (the release path itself, hardened before a second release could expose the download-address and delta bugs Teil B found and fixed by a local dry run). Publishing it is therefore also the only real proof that Teil B's own fix works against a *live* two-version beta channel, not only a throwaway local probe — the thing Teil B's own dry run could not by itself demonstrate, since nothing in a dry run ever reaches &lt;code&gt;Erikemmer/shelf-releases&lt;/code&gt;. 797 core tests, `make test&lt;code&gt;/&lt;/code&gt;make app&lt;code&gt;/&lt;/code&gt;make lint&lt;code&gt;/&lt;/code&gt;make smoke` all green against the bumped version.&lt;/p&gt;
&lt;h2&gt;Sprint 15, Teil A — the false "Could not read the library index" banner, and a sibling bug it led to · 23 September 2026&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;docs/BACKLOG.md&lt;/code&gt; carried this since Sprint 3: a visible "Could not read the library index" / &lt;code&gt;CancellationError&lt;/code&gt; banner could appear right after quitting cancelled an import — harmless, since nothing was ever lost, but wrong, and wrong at exactly the moment someone already fears for their library.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Root cause, found by reading the code rather than guessed:&lt;/strong&gt; &lt;code&gt;LibraryModel.runImport()&lt;/code&gt; awaited &lt;code&gt;reload()&lt;/code&gt; inside &lt;code&gt;importRunTask&lt;/code&gt; — the very &lt;code&gt;Task&lt;/code&gt; a Cancel click or a quit (&lt;code&gt;cancelImportRun()&lt;/code&gt;) had just cancelled. GRDB's async reads cooperatively check &lt;code&gt;Task.isCancelled&lt;/code&gt; and throw &lt;code&gt;CancellationError&lt;/code&gt; &lt;strong&gt;before touching the database at all&lt;/strong&gt; — the same check Sprint 13, Teil C's Bug 3 found silently losing a write. &lt;code&gt;reload()&lt;/code&gt;'s generic &lt;code&gt;catch&lt;/code&gt; turned that into a real-looking, but false, read-failure banner. The fix is the same one Bug 3 used: &lt;code&gt;runImport()&lt;/code&gt; now reloads through its own &lt;code&gt;Task.detached&lt;/code&gt;, which starts uncancelled regardless of what cancelled the caller — a *real* read failure (a locked or corrupt index) still surfaces; only the false one from cancellation does not.&lt;/p&gt;
&lt;p&gt;**Reproducing it live surfaced a second, sibling bug the first one had been masking.** With the false banner gone, cancelling a large enough import (4 000 synthetic books, large enough that a real Cancel click lands mid-copy) turned up a *different* false error, inside the sheet itself: "The operation couldn't be completed. (Swift.CancellationError error 1.)", with the sheet stuck on its Cancel button rather than reaching "Done". &lt;code&gt;ImportRunner.run()&lt;/code&gt;'s *regular*, mid-loop &lt;code&gt;saveBatch&lt;/code&gt; call — the one that fires every &lt;code&gt;indexBatchSize&lt;/code&gt; (200) books, not only the last short one — was never given Bug 3's own fix: it was still a plain `try await saveBatch(unsaved)`, and a Cancel landing exactly at that boundary let &lt;code&gt;CancellationError&lt;/code&gt; escape &lt;code&gt;run()&lt;/code&gt; itself, uncaught by anything, into &lt;code&gt;ImportModel.run()&lt;/code&gt;'s own &lt;code&gt;catch&lt;/code&gt;. Fixed the same way as the batch beside it: &lt;code&gt;try? await Task.detached { try await saveBatch(batch) }.value&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;**Tests, at the root rather than at the two call sites that cannot be unit-tested directly** (&lt;code&gt;LibraryModel&lt;/code&gt; has no app-side test target yet, and &lt;code&gt;ImportRunner&lt;/code&gt;'s own existing cancellation test uses an in-memory &lt;code&gt;BatchCollector&lt;/code&gt; that never checks cancellation, which is exactly why it never caught either bug): &lt;code&gt;Tests/ShelfCoreTests/IndexCancellationTests.swift&lt;/code&gt; proves, deterministically (an &lt;code&gt;AsyncStream&lt;/code&gt; gate, not a race), that a read *and* a write inside an already-cancelled &lt;code&gt;Task&lt;/code&gt; both throw &lt;code&gt;CancellationError&lt;/code&gt; against a perfectly healthy index, and that the same read and write through a &lt;code&gt;Task.detached&lt;/code&gt; both succeed regardless — the exact mechanism both fixes now rely on.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Live proof, both bugs, both languages, real clicks — never assumed:&lt;/strong&gt; &lt;code&gt;Scripts/import-cancel-proof.sh&lt;/code&gt; drives the real app: a real import, auto-started through &lt;code&gt;SHELF_AUTO_IMPORT_SOURCE&lt;/code&gt; (Sprint 13's own test hook), against 4 000 synthetic books; a real click on Cancel, found through the window's own accessibility tree; then the window's own accessibility tree read again for the banner's words. Reproduced first, against the code as it stood before this fix (&lt;code&gt;docs/screenshots/sprint-14-import-cancel/before-en-banner.jpg&lt;/code&gt;): the red banner, and the grid still reading "This library is empty" even though books had already been copied — the reload that would have shown them is exactly the one that failed. Then fixed, and the same script run again in both languages (&lt;code&gt;docs/screenshots/sprint-14-import-cancel/after-en-clean.jpg&lt;/code&gt;, &lt;code&gt;after-de-clean.jpg&lt;/code&gt;): no banner, the grid showing the 1 860 (English run) / 2 825 (German run) books actually copied before Cancel landed — different counts between runs because the exact moment a real click lands is not deterministic, and neither run is worse for it.&lt;/p&gt;
&lt;h2&gt;Sprint 14, Teil E — documentation catches up with the sprint · 23 September 2026&lt;/h2&gt;
&lt;p&gt;No code. &lt;code&gt;docs/RUNBOOK.md&lt;/code&gt; gets a new §15, "Releasing an update": the path &lt;code&gt;make release&lt;/code&gt; takes today (always builds, always publishes; signs for real once a Developer ID exists), where the Sparkle key lives and under which keychain account, what losing it would mean and why the keychain itself — not an exported file — is what has to be backed up, and the one thing that changes automatically once a Developer ID exists (nothing to edit by hand). &lt;code&gt;docs/ARCHITECTURE.md&lt;/code&gt; gets &lt;code&gt;UpdaterModel&lt;/code&gt; in the building blocks, a line on Sparkle being the app target's only other external dependency (never &lt;code&gt;ShelfCore&lt;/code&gt;), the delivery-is-separate-but-source-is-not shape ADR 0022 reasons, and a new "Data flow: checking for an update" section. &lt;code&gt;docs/HANDOFF.md&lt;/code&gt;'s top section is superseded by a new one describing &lt;code&gt;v1.1.0-rc1&lt;/code&gt;, kept rather than rewritten, the same layering convention &lt;code&gt;v1.0.0&lt;/code&gt;'s own report already used. &lt;code&gt;CLAUDE.md&lt;/code&gt; gets the two lines this whole sprint kept promising: the Sparkle key never goes into a repository, a file or a screenshot, and every Sparkle CLI call carries &lt;code&gt;--account shelf&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;**One thing checked rather than assumed, because Teil D's own instructions asked for it explicitly:** whether a comparable app already has a way for a *Release* build to ever reach its own beta channel that Shelf lacks. It does not, checked directly rather than assumed. Nothing added to &lt;code&gt;docs/BACKLOG.md&lt;/code&gt; for this, because there is no gap to record.&lt;/p&gt;
&lt;h2&gt;Sprint 14, Teil D — the version, past v1.0.0 on purpose · 23 September 2026&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;MARKETING_VERSION&lt;/code&gt; is &lt;code&gt;1.1.0-rc1&lt;/code&gt;, &lt;code&gt;CURRENT_PROJECT_VERSION&lt;/code&gt; is &lt;code&gt;2&lt;/code&gt; — Erik's own call, given directly rather than derived: &lt;code&gt;main&lt;/code&gt; is well past what &lt;code&gt;v1.0.0&lt;/code&gt; tags (writing into an EPUB's own file, "Remove Cover", a clean quit through every path, and now Sparkle itself), so publishing that work under the number &lt;code&gt;1.0.0&lt;/code&gt; would put a different app under the same version the tag already names. The rule against touching &lt;code&gt;MARKETING_VERSION&lt;/code&gt; outside a real release is lifted for this one change, as instructed, and only for this one. &lt;code&gt;1.1.0-rc1&lt;/code&gt; is a pre-release: &lt;code&gt;Scripts/release.sh&lt;/code&gt; routes any &lt;code&gt;-rc&lt;/code&gt; version to the beta channel (&lt;code&gt;appcast-beta.xml&lt;/code&gt;) with &lt;code&gt;--prerelease&lt;/code&gt;; &lt;code&gt;appcast.xml&lt;/code&gt;, for stable versions, stays exactly as HALT 1 seeded it. 793 core tests, `make app&lt;code&gt;/&lt;/code&gt;make lint&lt;code&gt;/&lt;/code&gt;make smoke` all green against the bumped version.&lt;/p&gt;
&lt;h2&gt;Sprint 14, Teil C — the update path proved live, over a throwaway test channel · 23 September 2026&lt;/h2&gt;
&lt;p&gt;Every step of Sparkle's own update path, driven for real rather than inferred from reading the code: an old (&lt;code&gt;1.0.0&lt;/code&gt;) Debug build, redirected via &lt;code&gt;SHELF_APPCAST_URL&lt;/code&gt; to a fake &lt;code&gt;appcast-test.xml&lt;/code&gt; (version &lt;code&gt;9.9.9&lt;/code&gt;, committed to the public &lt;code&gt;Erikemmer/shelf-releases&lt;/code&gt; and reached the same way the real feed is, over &lt;code&gt;raw.githubusercontent.com&lt;/code&gt; — possible only because that repo is public, unlike an equivalent private one, which would have no choice but a fully local server for everything), found and installed a second local build served from a local HTTP server on this Mac. Every click that matters was a real one: &lt;code&gt;Scripts/click-at.swift&lt;/code&gt; at a position read off the accessibility tree, &lt;code&gt;Scripts/menu-point.swift&lt;/code&gt; for the item inside an already-open menu.&lt;/p&gt;
&lt;p&gt;**One gap in the existing tooling, found and closed before it could block the proof:** &lt;code&gt;Scripts/menu-point.swift&lt;/code&gt; explicitly skips the whole &lt;code&gt;AXMenuBar&lt;/code&gt; subtree — right for the in-window pop-up menus every other script here drives (its own comment says why), and exactly wrong for the *application* menu bar's own open dropdown, which sits nested inside that very subtree. A new scratch-only helper (this session's own, not part of the repo — Teil C's proof is throwaway by design) walked down from the named menu bar item instead of skipping it, and found `Nach Updates suchen …` (German, matching this Mac's locale, and Sparkle's own dialogs turned out to be German too, unprompted).&lt;/p&gt;
&lt;p&gt;**What the four screenshots show, each with the real click that produced it:** the notice and its changelist together (Sparkle's own dialog names the version and renders the fake HTML release notes from &lt;code&gt;appcast-test.xml&lt;/code&gt;); "Bereit zum Installieren" once the download and signature check both passed (too fast, over a local server, to catch a separate mid-download frame — noted rather than staged); the relaunch, a new pid running from the exact same bundle path; and `spctl -a -vvv -t execute&lt;code&gt; on that same, now-updated bundle answering &lt;strong&gt;&lt;/code&gt;rejected`&lt;/strong&gt; — expected, and not worked around, exactly as ADR 0022 says it should be until a Developer ID exists.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Verified by reading files back, not by trusting the window:&lt;/strong&gt; the updated bundle's own &lt;code&gt;Info.plist&lt;/code&gt;, at the identical path the old build was copied to, read &lt;code&gt;CFBundleShortVersionString&lt;/code&gt; &lt;code&gt;9.9.9&lt;/code&gt; and &lt;code&gt;CFBundleVersion&lt;/code&gt; &lt;code&gt;999&lt;/code&gt; after the relaunch — Sparkle really replaced the app bundle in place, checked on disk rather than taken on the window's word.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Removed afterwards, restlos:&lt;/strong&gt; &lt;code&gt;appcast-test.xml&lt;/code&gt; deleted from &lt;code&gt;shelf-releases&lt;/code&gt; and pushed (confirmed &lt;code&gt;appcast.xml&lt;/code&gt;/&lt;code&gt;appcast-beta.xml&lt;/code&gt; were never touched by this test — one commit in their history, the seed from HALT 1); both test app copies, the zip, and every screenshot under &lt;code&gt;~/Library/Caches/Shelf/update-test/&lt;/code&gt; (64 MB, &lt;code&gt;rm -rf&lt;/code&gt;, named here because this session created every byte of it); the local HTTP server (pid, started by this session) and the relaunched test client (pid, likewise) both stopped by this session, not left running. &lt;code&gt;project.yml&lt;/code&gt; was restored to &lt;code&gt;1.0.0&lt;/code&gt;/&lt;code&gt;1&lt;/code&gt; immediately after the fake build was copied out — &lt;code&gt;git status --short&lt;/code&gt; confirms clean before this commit. Nothing under &lt;code&gt;Erikemmer/Shelf&lt;/code&gt; itself changed; this whole Teil was proof, not code.&lt;/p&gt;
&lt;h2&gt;Sprint 14, Teil B — &lt;code&gt;make release&lt;/code&gt; publishes, and no longer refuses without a Developer ID · 23 September 2026&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;Scripts/release.sh&lt;/code&gt; gains a publish phase and one real behaviour change to the path before it.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;A missing Developer ID used to stop a real (non-dry) run outright&lt;/strong&gt; — fixed at the same commit &lt;code&gt;RELEASE_DRY_RUN=1&lt;/code&gt; was documented as the workaround for exactly that. Now it signs ad hoc instead, skips notarising and stapling (saying so, and running &lt;code&gt;spctl&lt;/code&gt; anyway "for the record" — expected &lt;code&gt;rejected&lt;/code&gt;), names the download &lt;code&gt;Shelf-&amp;lt;version&amp;gt;-unsigned.zip&lt;/code&gt;, and &lt;strong&gt;still publishes&lt;/strong&gt; — exactly the path &lt;code&gt;v1.0.0&lt;/code&gt; went out on by hand once, now the one this script always takes until a certificate exists. &lt;code&gt;RELEASE_DRY_RUN=1&lt;/code&gt; is unchanged and is the only path that never publishes, whatever is or is not in the keychain.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Publishing&lt;/strong&gt; (only for a real run): &lt;code&gt;sign_update --account shelf&lt;/code&gt; on the zip, release notes rendered from &lt;code&gt;CHANGELOG.md&lt;/code&gt; (&lt;code&gt;Scripts/changelog-notes.py&lt;/code&gt;, new), &lt;code&gt;generate_appcast --account shelf&lt;/code&gt; against a per-channel archive directory that accumulates across releases (&lt;code&gt;~/Library/Caches/Shelf/appcast-archives/{stable,beta}&lt;/code&gt;), a GitHub release in &lt;code&gt;Erikemmer/shelf-releases&lt;/code&gt; (&lt;code&gt;--prerelease&lt;/code&gt; and &lt;code&gt;appcast-beta.xml&lt;/code&gt; for an &lt;code&gt;-rc&lt;/code&gt; version, &lt;code&gt;appcast.xml&lt;/code&gt; otherwise), and the updated appcast committed and pushed there. &lt;code&gt;--account shelf&lt;/code&gt; on every one of the three Sparkle CLI calls this script and &lt;code&gt;generate_keys&lt;/code&gt; make between them — never the default, for the reason ADR 0022 and Teil A found.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;The CHANGELOG.md marker convention.&lt;/strong&gt; Shelf's own changelog is not keyed by version (&lt;code&gt;## Sprint 14, Teil B&lt;/code&gt;, not &lt;code&gt;## [1.1.0-rc1]&lt;/code&gt;) — so "the matching section" for a release cannot be found by matching a version string. &lt;code&gt;changelog-notes.py&lt;/code&gt; instead reads everything newest-first down to an HTML comment, `&lt;/li&gt;
&lt;/ul&gt;
</description>
            <enclosure url="https://github.com/Erikemmer/shelf-releases/releases/download/v1.1.0-rc2/Shelf-1.1.0-rc2-unsigned.zip" length="7018976" type="application/octet-stream" sparkle:edSignature="SP8T7b0gIOr3DTj2aAqRa43DCJVnzKV2yffwXuRcb0xk+i3X9E6CHjebBIXZYAL/Xd+x+PTP5CMPxnsMv9oyAg==" />
        </item>
        <item>
            <title>1.1.0-rc1</title>
            <pubDate>Wed, 23 Sep 2026 14:15:56 +0200</pubDate>
            <sparkle:version>2</sparkle:version>
            <sparkle:shortVersionString>1.1.0-rc1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description>&lt;h2&gt;Shelf 1.1.0-rc1&lt;/h2&gt;
&lt;h2&gt;Sprint 14, Teil D — the version, past v1.0.0 on purpose · 23 September 2026&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;MARKETING_VERSION&lt;/code&gt; is &lt;code&gt;1.1.0-rc1&lt;/code&gt;, &lt;code&gt;CURRENT_PROJECT_VERSION&lt;/code&gt; is &lt;code&gt;2&lt;/code&gt; — Erik's own call, given directly rather than derived: &lt;code&gt;main&lt;/code&gt; is well past what &lt;code&gt;v1.0.0&lt;/code&gt; tags (writing into an EPUB's own file, "Remove Cover", a clean quit through every path, and now Sparkle itself), so publishing that work under the number &lt;code&gt;1.0.0&lt;/code&gt; would put a different app under the same version the tag already names. The rule against touching &lt;code&gt;MARKETING_VERSION&lt;/code&gt; outside a real release is lifted for this one change, as instructed, and only for this one. &lt;code&gt;1.1.0-rc1&lt;/code&gt; is a pre-release: &lt;code&gt;Scripts/release.sh&lt;/code&gt; routes any &lt;code&gt;-rc&lt;/code&gt; version to the beta channel (&lt;code&gt;appcast-beta.xml&lt;/code&gt;) with &lt;code&gt;--prerelease&lt;/code&gt;; &lt;code&gt;appcast.xml&lt;/code&gt;, for stable versions, stays exactly as HALT 1 seeded it. 793 core tests, `make app&lt;code&gt;/&lt;/code&gt;make lint&lt;code&gt;/&lt;/code&gt;make smoke` all green against the bumped version.&lt;/p&gt;
&lt;h2&gt;Sprint 14, Teil C — the update path proved live, over a throwaway test channel · 23 September 2026&lt;/h2&gt;
&lt;p&gt;Every step of Sparkle's own update path, driven for real rather than inferred from reading the code: an old (&lt;code&gt;1.0.0&lt;/code&gt;) Debug build, redirected via &lt;code&gt;SHELF_APPCAST_URL&lt;/code&gt; to a fake &lt;code&gt;appcast-test.xml&lt;/code&gt; (version &lt;code&gt;9.9.9&lt;/code&gt;, committed to the public &lt;code&gt;Erikemmer/shelf-releases&lt;/code&gt; and reached the same way the real feed is, over &lt;code&gt;raw.githubusercontent.com&lt;/code&gt; — unlike Selector's own private-repo test channel, which had no choice but a fully local server for everything), found and installed a second local build served from a local HTTP server on this Mac. Every click that matters was a real one: &lt;code&gt;Scripts/click-at.swift&lt;/code&gt; at a position read off the accessibility tree, &lt;code&gt;Scripts/menu-point.swift&lt;/code&gt; for the item inside an already-open menu.&lt;/p&gt;
&lt;p&gt;**One gap in the existing tooling, found and closed before it could block the proof:** &lt;code&gt;Scripts/menu-point.swift&lt;/code&gt; explicitly skips the whole &lt;code&gt;AXMenuBar&lt;/code&gt; subtree — right for the in-window pop-up menus every other script here drives (its own comment says why), and exactly wrong for the *application* menu bar's own open dropdown, which sits nested inside that very subtree. A new scratch-only helper (this session's own, not part of the repo — Teil C's proof is throwaway by design) walked down from the named menu bar item instead of skipping it, and found `Nach Updates suchen …` (German, matching this Mac's locale, and Sparkle's own dialogs turned out to be German too, unprompted).&lt;/p&gt;
&lt;p&gt;**What the four screenshots show, each with the real click that produced it:** the notice and its changelist together (Sparkle's own dialog names the version and renders the fake HTML release notes from &lt;code&gt;appcast-test.xml&lt;/code&gt;); "Bereit zum Installieren" once the download and signature check both passed (too fast, over a local server, to catch a separate mid-download frame — noted rather than staged); the relaunch, a new pid running from the exact same bundle path; and `spctl -a -vvv -t execute&lt;code&gt; on that same, now-updated bundle answering &lt;strong&gt;&lt;/code&gt;rejected`&lt;/strong&gt; — expected, and not worked around, exactly as ADR 0022 says it should be until a Developer ID exists.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Verified by reading files back, not by trusting the window:&lt;/strong&gt; the updated bundle's own &lt;code&gt;Info.plist&lt;/code&gt;, at the identical path the old build was copied to, read &lt;code&gt;CFBundleShortVersionString&lt;/code&gt; &lt;code&gt;9.9.9&lt;/code&gt; and &lt;code&gt;CFBundleVersion&lt;/code&gt; &lt;code&gt;999&lt;/code&gt; after the relaunch — Sparkle really replaced the app bundle in place, the same discipline Selector's own proof used ("per Datei nachgelesen, nicht nur der Oberfläche geglaubt").&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Removed afterwards, restlos:&lt;/strong&gt; &lt;code&gt;appcast-test.xml&lt;/code&gt; deleted from &lt;code&gt;shelf-releases&lt;/code&gt; and pushed (confirmed &lt;code&gt;appcast.xml&lt;/code&gt;/&lt;code&gt;appcast-beta.xml&lt;/code&gt; were never touched by this test — one commit in their history, the seed from HALT 1); both test app copies, the zip, and every screenshot under &lt;code&gt;~/Library/Caches/Shelf/update-test/&lt;/code&gt; (64 MB, &lt;code&gt;rm -rf&lt;/code&gt;, named here because this session created every byte of it); the local HTTP server (pid, started by this session) and the relaunched test client (pid, likewise) both stopped by this session, not left running. &lt;code&gt;project.yml&lt;/code&gt; was restored to &lt;code&gt;1.0.0&lt;/code&gt;/&lt;code&gt;1&lt;/code&gt; immediately after the fake build was copied out — &lt;code&gt;git status --short&lt;/code&gt; confirms clean before this commit. Nothing under &lt;code&gt;Erikemmer/Shelf&lt;/code&gt; itself changed; this whole Teil was proof, not code.&lt;/p&gt;
&lt;h2&gt;Sprint 14, Teil B — &lt;code&gt;make release&lt;/code&gt; publishes, and no longer refuses without a Developer ID · 23 September 2026&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;Scripts/release.sh&lt;/code&gt; gains a publish phase and one real behaviour change to the path before it.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;A missing Developer ID used to stop a real (non-dry) run outright&lt;/strong&gt; — fixed at the same commit &lt;code&gt;RELEASE_DRY_RUN=1&lt;/code&gt; was documented as the workaround for exactly that. Now it signs ad hoc instead, skips notarising and stapling (saying so, and running &lt;code&gt;spctl&lt;/code&gt; anyway "for the record" — expected &lt;code&gt;rejected&lt;/code&gt;), names the download &lt;code&gt;Shelf-&amp;lt;version&amp;gt;-unsigned.zip&lt;/code&gt;, and &lt;strong&gt;still publishes&lt;/strong&gt; — exactly the path &lt;code&gt;v1.0.0&lt;/code&gt; went out on by hand once, now the one this script always takes until a certificate exists. &lt;code&gt;RELEASE_DRY_RUN=1&lt;/code&gt; is unchanged and is the only path that never publishes, whatever is or is not in the keychain.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Publishing&lt;/strong&gt; (only for a real run): &lt;code&gt;sign_update --account shelf&lt;/code&gt; on the zip, release notes rendered from &lt;code&gt;CHANGELOG.md&lt;/code&gt; (&lt;code&gt;Scripts/changelog-notes.py&lt;/code&gt;, new), &lt;code&gt;generate_appcast --account shelf&lt;/code&gt; against a per-channel archive directory that accumulates across releases (&lt;code&gt;~/Library/Caches/Shelf/appcast-archives/{stable,beta}&lt;/code&gt;), a GitHub release in &lt;code&gt;Erikemmer/shelf-releases&lt;/code&gt; (&lt;code&gt;--prerelease&lt;/code&gt; and &lt;code&gt;appcast-beta.xml&lt;/code&gt; for an &lt;code&gt;-rc&lt;/code&gt; version, &lt;code&gt;appcast.xml&lt;/code&gt; otherwise), and the updated appcast committed and pushed there. &lt;code&gt;--account shelf&lt;/code&gt; on every one of the three Sparkle CLI calls this script and &lt;code&gt;generate_keys&lt;/code&gt; make between them — never the default, for the reason ADR 0022 and Teil A found.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;The CHANGELOG.md marker convention.&lt;/strong&gt; Shelf's own changelog is not keyed by version (&lt;code&gt;## Sprint 14, Teil B&lt;/code&gt;, not &lt;code&gt;## [1.1.0-rc1]&lt;/code&gt;), unlike Selector's — so "the matching section" for a release cannot be found by matching a version string. &lt;code&gt;changelog-notes.py&lt;/code&gt; instead reads everything newest-first down to an HTML comment, `&lt;/li&gt;
&lt;/ul&gt;
</description>
            <enclosure url="https://github.com/Erikemmer/shelf-releases/releases/download/v1.1.0-rc1/Shelf-1.1.0-rc1-unsigned.zip" length="7013189" type="application/octet-stream" sparkle:edSignature="6O1HtSt7Vot7/Ez17g0Adagt4n1bmXt/gYLZPoOzxCAieJWa7lnvjqHOoGN9Y6/AdJMJxsshbiU91y6UbsB0Cw==" />
        </item>
    </channel>
</rss>