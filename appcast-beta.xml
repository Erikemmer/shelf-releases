<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>Shelf (beta)</title>
        <link>https://raw.githubusercontent.com/Erikemmer/shelf-releases/main/appcast-beta.xml</link>
        <description>Pre-release (rc) builds of Shelf.</description>
        <language>en</language>
        <!-- No pre-release has been published yet. make release fills this in for an rc version. -->
        <item>
            <title>1.1.0-rc1</title>
            <pubDate>Wed, 23 Sep 2026 14:15:56 +0200</pubDate>
            <sparkle:version>2</sparkle:version>
            <sparkle:shortVersionString>1.1.0-rc1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h2>Shelf 1.1.0-rc1</h2>
<h2>Sprint 14, Teil D — the version, past v1.0.0 on purpose · 23 September 2026</h2>
<p><code>MARKETING_VERSION</code> is <code>1.1.0-rc1</code>, <code>CURRENT_PROJECT_VERSION</code> is <code>2</code> — Erik's own call, given directly rather than derived: <code>main</code> is well past what <code>v1.0.0</code> tags (writing into an EPUB's own file, "Remove Cover", a clean quit through every path, and now Sparkle itself), so publishing that work under the number <code>1.0.0</code> would put a different app under the same version the tag already names. The rule against touching <code>MARKETING_VERSION</code> outside a real release is lifted for this one change, as instructed, and only for this one. <code>1.1.0-rc1</code> is a pre-release: <code>Scripts/release.sh</code> routes any <code>-rc</code> version to the beta channel (<code>appcast-beta.xml</code>) with <code>--prerelease</code>; <code>appcast.xml</code>, for stable versions, stays exactly as HALT 1 seeded it. 793 core tests, `make app<code>/</code>make lint<code>/</code>make smoke` all green against the bumped version.</p>
<h2>Sprint 14, Teil C — the update path proved live, over a throwaway test channel · 23 September 2026</h2>
<p>Every step of Sparkle's own update path, driven for real rather than inferred from reading the code: an old (<code>1.0.0</code>) Debug build, redirected via <code>SHELF_APPCAST_URL</code> to a fake <code>appcast-test.xml</code> (version <code>9.9.9</code>, committed to the public <code>Erikemmer/shelf-releases</code> and reached the same way the real feed is, over <code>raw.githubusercontent.com</code> — unlike Selector's own private-repo test channel, which had no choice but a fully local server for everything), found and installed a second local build served from a local HTTP server on this Mac. Every click that matters was a real one: <code>Scripts/click-at.swift</code> at a position read off the accessibility tree, <code>Scripts/menu-point.swift</code> for the item inside an already-open menu.</p>
<p>**One gap in the existing tooling, found and closed before it could block the proof:** <code>Scripts/menu-point.swift</code> explicitly skips the whole <code>AXMenuBar</code> subtree — right for the in-window pop-up menus every other script here drives (its own comment says why), and exactly wrong for the *application* menu bar's own open dropdown, which sits nested inside that very subtree. A new scratch-only helper (this session's own, not part of the repo — Teil C's proof is throwaway by design) walked down from the named menu bar item instead of skipping it, and found `Nach Updates suchen …` (German, matching this Mac's locale, and Sparkle's own dialogs turned out to be German too, unprompted).</p>
<p>**What the four screenshots show, each with the real click that produced it:** the notice and its changelist together (Sparkle's own dialog names the version and renders the fake HTML release notes from <code>appcast-test.xml</code>); "Bereit zum Installieren" once the download and signature check both passed (too fast, over a local server, to catch a separate mid-download frame — noted rather than staged); the relaunch, a new pid running from the exact same bundle path; and `spctl -a -vvv -t execute<code> on that same, now-updated bundle answering <strong></code>rejected`</strong> — expected, and not worked around, exactly as ADR 0022 says it should be until a Developer ID exists.</p>
<p><strong>Verified by reading files back, not by trusting the window:</strong> the updated bundle's own <code>Info.plist</code>, at the identical path the old build was copied to, read <code>CFBundleShortVersionString</code> <code>9.9.9</code> and <code>CFBundleVersion</code> <code>999</code> after the relaunch — Sparkle really replaced the app bundle in place, the same discipline Selector's own proof used ("per Datei nachgelesen, nicht nur der Oberfläche geglaubt").</p>
<p><strong>Removed afterwards, restlos:</strong> <code>appcast-test.xml</code> deleted from <code>shelf-releases</code> and pushed (confirmed <code>appcast.xml</code>/<code>appcast-beta.xml</code> were never touched by this test — one commit in their history, the seed from HALT 1); both test app copies, the zip, and every screenshot under <code>~/Library/Caches/Shelf/update-test/</code> (64 MB, <code>rm -rf</code>, named here because this session created every byte of it); the local HTTP server (pid, started by this session) and the relaunched test client (pid, likewise) both stopped by this session, not left running. <code>project.yml</code> was restored to <code>1.0.0</code>/<code>1</code> immediately after the fake build was copied out — <code>git status --short</code> confirms clean before this commit. Nothing under <code>Erikemmer/Shelf</code> itself changed; this whole Teil was proof, not code.</p>
<h2>Sprint 14, Teil B — <code>make release</code> publishes, and no longer refuses without a Developer ID · 23 September 2026</h2>
<p><code>Scripts/release.sh</code> gains a publish phase and one real behaviour change to the path before it.</p>
<ul>
<li><strong>A missing Developer ID used to stop a real (non-dry) run outright</strong> — fixed at the same commit <code>RELEASE_DRY_RUN=1</code> was documented as the workaround for exactly that. Now it signs ad hoc instead, skips notarising and stapling (saying so, and running <code>spctl</code> anyway "for the record" — expected <code>rejected</code>), names the download <code>Shelf-&lt;version&gt;-unsigned.zip</code>, and <strong>still publishes</strong> — exactly the path <code>v1.0.0</code> went out on by hand once, now the one this script always takes until a certificate exists. <code>RELEASE_DRY_RUN=1</code> is unchanged and is the only path that never publishes, whatever is or is not in the keychain.</li>
<li><strong>Publishing</strong> (only for a real run): <code>sign_update --account shelf</code> on the zip, release notes rendered from <code>CHANGELOG.md</code> (<code>Scripts/changelog-notes.py</code>, new), <code>generate_appcast --account shelf</code> against a per-channel archive directory that accumulates across releases (<code>~/Library/Caches/Shelf/appcast-archives/{stable,beta}</code>), a GitHub release in <code>Erikemmer/shelf-releases</code> (<code>--prerelease</code> and <code>appcast-beta.xml</code> for an <code>-rc</code> version, <code>appcast.xml</code> otherwise), and the updated appcast committed and pushed there. <code>--account shelf</code> on every one of the three Sparkle CLI calls this script and <code>generate_keys</code> make between them — never the default, for the reason ADR 0022 and Teil A found.</li>
<li><strong>The CHANGELOG.md marker convention.</strong> Shelf's own changelog is not keyed by version (<code>## Sprint 14, Teil B</code>, not <code>## [1.1.0-rc1]</code>), unlike Selector's — so "the matching section" for a release cannot be found by matching a version string. <code>changelog-notes.py</code> instead reads everything newest-first down to an HTML comment, `</li>
</ul>
]]></description>
            <enclosure url="https://github.com/Erikemmer/shelf-releases/releases/download/v1.1.0-rc1/Shelf-1.1.0-rc1-unsigned.zip" length="7013189" type="application/octet-stream" sparkle:edSignature="6O1HtSt7Vot7/Ez17g0Adagt4n1bmXt/gYLZPoOzxCAieJWa7lnvjqHOoGN9Y6/AdJMJxsshbiU91y6UbsB0Cw=="/>
        </item>
    </channel>
</rss>